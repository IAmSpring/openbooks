import defaultUserNotes from './defaultUserNotes.json';
import defaultConfigs from './defaultConfigs.json';
import defaultFiles from './defaultFiles.json';

export const defaultData = {
    userNotes: defaultUserNotes,
    configs: defaultConfigs,
    files: defaultFiles
};

export default defaultData; 